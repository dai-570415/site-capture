import { Layout } from '../components/Layout/Layout';
import { SiteCapture } from '../components/SiteCapture/SiteCapture';

export const Index = () => {
    return (
        <Layout>
            <SiteCapture />
        </Layout>
    );
}